import Foundation

enum AppConfig {
    static let shotAnalysisURL = URL(string: "https://www.viasion.app")!
    static let apiBaseURL = URL(string: "https://api.viasion.app")!
    static let registrationPath = "api/auth/register"
    static let loginURL = URL(string: "https://www.viasion.app/login")!
}
